{
  "$schema": "http://json-schema.org/draft-04/schema#",

  "title": "Get user details /v1/users/{ids}",
  "type":  "object",

  "properties": {
    "users": {
      "type":  "array",
      "items": { "$ref": "../_models/user.json" },
      "minItems": 1,
      "maxItems": 100
    }
  },

  "required": ["users"],
  "additionalProperties": false
}
